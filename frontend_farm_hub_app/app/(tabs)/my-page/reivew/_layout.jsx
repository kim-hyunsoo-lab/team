import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { Stack } from 'expo-router'

const ReviewMyPageLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown : false
      }}
    />
  )
}

export default ReviewMyPageLayout

const styles = StyleSheet.create({})